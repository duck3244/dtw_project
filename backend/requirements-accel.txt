# Optional acceleration dependencies (A안: accel backend)
# Install in addition to requirements.txt to enable the accel backend:
#   pip install -r requirements.txt -r requirements-accel.txt

numba>=0.58
# torch / torchaudio are optional; only required for GPU batched DTW
# and torchaudio-based feature extraction. Comment out if not needed.
torch>=2.0
torchaudio>=2.0
joblib>=1.3
tqdm>=4.65
